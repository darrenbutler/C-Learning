#pragma once

//Make precompiled heaeder later. For now, The class definition will be in the 
//same file as the client code
/*
class Dog
{
private:
    double weight;//attributes
public:
    Rectangle();//Default constructor

    // Parameterized constructor
    Rectangle(double l, double w);
    // Getter functions 
    double getLength();
    double getWidth();
    // Setter functions
    void setLength(double);
    void setWidth(double);
    double perimeter();
    double area();

    //Destructor
    ~Rectangle();
    */